import { DivisaDirective } from './divisa.directive';

describe('DivisaDirective', () => {
  it('should create an instance', () => {
    const directive = new DivisaDirective();
    expect(directive).toBeTruthy();
  });
});
